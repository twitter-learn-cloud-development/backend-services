apiVersion: v1
kind: ConfigMap
metadata:
  name: twitter-clone-dashboard
  namespace: monitoring
  labels:
    {{- include "twitter.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  twitter-clone-dashboard.json: |-
{{ .Files.Get "dashboards/twitter-clone-dashboard.json" | nindent 4 }}
